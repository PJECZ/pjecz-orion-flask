{% extends 'personas/detail_datos_generales.jinja2' %}

{% block content %}
    {{ super() }}
    <!-- Ver Actas Protestas -->
    {% if current_user.can_view("PERSONAS ACTAS PROTESTAS") %}
    {% call detail.card("Listado de Actas de Protestas") %}
        <!-- DataTable Actas Protestas -->
        <table id="personas_actas_protestas_datatable" class="table display nowrap" style="width:100%">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Fecha</th>
                    <th>Cargo</th>
                </tr>
            </thead>
        </table>
        <a href="{{url_for('personas_actas_protestas.new_with_persona_id', persona_id=persona.id)}}" class="btn btn-primary">Agregar Acta de Protesta</a>
    {% endcall %}
    {% endif %}
{% endblock %}

{% block custom_javascript %}
    {{ super() }}
    <!-- Activar btn de sección -->
    <script>
        const boton = document.getElementById('btn_actas_protestas');
        boton.classList.add('active');
        boton.href = "{{url_for('personas.detail', persona_id=persona.id)}}";
    </script>
    <!-- DataTables -->
    <script src="/static/js/datatables-constructor.js"></script>
    <script src="/static/js/datatables-filtros.js"></script>
    {% if current_user.can_view("PERSONAS ACTAS PROTESTAS") %}
        <script>
            // DataTable Actas Protestas
            const constructorDataTable = new ConfigDataTable( '{{ csrf_token() }}' );
            let configDataTable = constructorDataTable.config();
            configDataTable['ajax']['url'] = '/personas_actas_protestas/datatable_json';
            configDataTable['ajax']['data'] = {"estatus": "A", "persona_id": {{ persona.id }}};
            configDataTable['columns'] = [
                { data: 'detalle' },
                { data: 'fecha' },
                { data: 'cargo' }
            ];
            configDataTable['columnDefs'] = [
                {
                    targets: 0, // detalle
                    data: null,
                    render: function(data, type, row, meta) {
                        return '<a href="' + data.url + '">' + data.id + '</a>';
                    }
                }
            ];
            // Filtros ActasProtestas
            const filtrosActasProtestas = new FiltrosDataTable('#personas_actas_protestas_datatable', configDataTable);
            filtrosActasProtestas.precargar();
        </script>
    {% endif %}
{% endblock %}